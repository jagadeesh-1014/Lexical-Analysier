# Lexical-Analysier
Designed and implemented a lexical analyzer for a C-like language that scans source code and converts it into tokens such as keywords, identifiers, constants, operators, and preprocessor directives, demonstrating core compiler front-end and parsing concepts.
